package Enums;
/**Enum {@linkplain OrderType} {Liquid,Solid,Mix} Order can be prepared of liquid products (coffee, tea, milk) or solid (
 * meals, breakfast and so on) or can be mixed.
 */
public enum OrderType {
    Liquid,Solid,Mix
}